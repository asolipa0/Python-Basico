$(function(){var $el,$ps,$up,totalHeight,$previosHeight,$status;$(".syllabus-box .button").click(function(){totalHeight=0;$previosHeight=0;$el=$(this);$up=$el.parent();$ps=$up.find("p:not('.read-more')");$previosHeight+=$ps.prevObject.height()+30;totalHeight+=$ps.prevObject[0].scrollHeight+30;$status=$up.data('value');if($status==="closed"){$up.css({"height":$up.height(),"max-height":"100%",}).animate({"height":totalHeight}).data({value:'open',previousHeight:$previosHeight}).find('div.gradient').fadeOut();$el.data("old_text",$el.text())
$el.text("Mostrar menos");}else if($status==="open"){$up.css({"height":$up.height(),"max-height":"100%",}).animate({"height":$up.data('previousHeight')}).data({value:'closed'}).find('div.gradient').fadeIn();$el.text($el.data("old_text"));}
return false;});});